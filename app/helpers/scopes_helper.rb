module ScopesHelper

  # def scope_link *args
  #   scopes = args.extract_options!
  #   scope_params = {}
  #   scopes.each do |name, value|
  #     scope_params[name] = value
  #   end
  #   link_to name.humanize, params.merge(scope_params)
  # end

end
