- if object.errors.any?
  #error_explanation
    %h2= "#{pluralize(object.errors.count, "error")} prohibited this company from being saved:"
    - object.errors.full_messages.each do |msg|
      = msg